
MODEL:
  NAME: "vssm"
  DEVICE_ID: "0"
  PRETRAIN_CHOICE: "imagenet"
  PRETRAIN_PATH: "/home/user/Project/TransReID/TransReID/vssm_base_0229_ckpt_epoch_237.pth"
  DROP_PATH_RATE: 0.6

  NECK: "bnneck"
  COS_LAYER: False
  ID_LOSS_TYPE: "softmax"
  METRIC_LOSS_TYPE: "triplet"
  IF_LABELSMOOTH: "off"
  IF_WITH_CENTER: "no"
  NO_MARGIN: False
  DIST_TRAIN: False

  VSSM:
    IN_CHANS: 3
    PATCH_SIZE: 4
    EMBED_DIM: 128

    # ===== 반드시 ckpt와 일치 =====
    DEPTHS: [2, 2, 15, 2]
    SSM_RATIO: 2.0
    # ============================
    
    # SSM_D_STATE: 1
    # SSM_DT_RANK: "auto"
    # SSM_CONV: 3
    # SSM_CONV_BIAS: False
    # SSM_FORWARDTYPE: "v05_noz"
    # SSM_ACT_LAYER: "silu"
    # SSM_DROP_RATE: 0.0
    # SSM_INIT: "v0"

    # MLP_ACT_LAYER: "gelu"
    # MLP_DROP_RATE: 0.0

    # PATCH_NORM: True
    # POSEMBED: False
    # GMLP: False
    # MLP_RATIO: 4.0
    # DOWNSAMPLE: "v3"
    # PATCHEMBED: "v2"
    # NORM_LAYER: "ln2d"
    SSM_D_STATE: 1
    SSM_DT_RANK: "auto"
    SSM_CONV: 3
    SSM_CONV_BIAS: False
    SSM_FORWARDTYPE: "v05_noz"
    MLP_RATIO: 4.0
    DOWNSAMPLE: "v3"
    PATCHEMBED: "v2"
    NORM_LAYER: "ln2d"


INPUT:
  SIZE_TRAIN: [256, 128]
  SIZE_TEST:  [256, 128]
  PROB: 0.5
  RE_PROB: 0.25
  PADDING: 10
  PIXEL_MEAN: [0.485, 0.456, 0.406]
  PIXEL_STD:  [0.229, 0.224, 0.225]

DATASETS:
  NAMES: "msmt17"
  ROOT_DIR: "/home/user/Project/TransReID/data"

DATALOADER:
  SAMPLER: "softmax_triplet"
  NUM_INSTANCE: 4
  NUM_WORKERS: 8

SOLVER:
  OPTIMIZER_NAME: "AdamW"
  MAX_EPOCHS: 50
  BASE_LR: 1e-4
  IMS_PER_BATCH: 32
  WARMUP_EPOCHS: 10
  WARMUP_METHOD: "linear"
  LARGE_FC_LR: False
  BIAS_LR_FACTOR: 1
  MOMENTUM: 0.9
  WEIGHT_DECAY: 0.01
  WEIGHT_DECAY_BIAS: 0.0
  CHECKPOINT_PERIOD: 10
  LOG_PERIOD: 100
  EVAL_PERIOD: 50

TEST:
  EVAL: True
  IMS_PER_BATCH: 128
  RE_RANKING: True
  WEIGHT: ""
  NECK_FEAT: "before"
  FEAT_NORM: "yes"

OUTPUT_DIR: "../logs/msmt17_vssm_10"
